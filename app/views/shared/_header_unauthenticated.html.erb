<% unless current_user %>
  <nav class="navigation">
    <%= render :partial => "shared/account_info" %>
    <div class="navigation__items">
      <%= link_to(t("identities.new.submit"), sign_up_path, class: "navigation__items__item", title: t("identities.new.submit")) if $switch_board.sign_up_active? %>
      <%= link_to(t("sessions.new.submit"), sign_in_path, class: "navigation__items__item", title: t("sessions.new.submit")) %>
    </div>
  </nav>
<% end %>
